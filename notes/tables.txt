colors
        c                 varchar(20)                color names
        restriction tinyint(1)        0 if color is free to make, 1 if it is restricted, 2 if it is species-exclusive
        
species
        s                 varchar(20)                species names
        restriction        tinyint(1)        0 if pet is free to make, 1 if it is restricted/le
        
pets
        name        varchar(20)                primary key
        owner        int(11)                        uid
        species        varchar(20)
        color        varchar(20)
        dob                timestamp                INPUT NULL
        rank        tinyint(3)
        gender        enum                        male female unknown undisclosed
        
        
accounts
        uid                smallint(5)         primary key
        urank        enum()                        user/mod/admin/banned
        uname        varchar(20)                username UNIQUE
        pass        varchar(20)                password
        joindate        date                 date of signup
        gender        
        numpets        tinyint(3)                # of pets on account
        maxpets        tinyint(3)                total # of petslots
        activepet
        money
        
        
items
        ID                 int(11)                        primary key
        owner         int(11)                        account id of player
        name         varchar(32)                name of item
        location enum                         where in account (inventory, sdb, gallery)
        category        varchar(20)        category
        
        
morphpot
        name                 varchar(32)                primary key
        rarity                 tinyint (3)                restock amount
        value                 int(10)                        resell value for NPC
        description text                        item description for inventory
        morphcolor         varchar(20)                new pet color
        morphspecies varchar(20)        new pet species
        imgurl                 varchar(100)        URL of image 
        
paintbrush
        name                 varchar(32)                primary key
        rarity                 tinyint (3)                restock amount
        value                 int(10)                        resell value for NPC
        description text                        item description for inventory
        paintcolor         varchar(20)                new pet color
        imgurl                 varchar(100)        URL of image 
        
        
petpages
        petname
        content
        lastupdate